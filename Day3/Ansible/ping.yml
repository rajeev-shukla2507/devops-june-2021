- name: First Play
  hosts: ubuntu
  tasks:
   - name: Ping ubuntu ansible nodes
     ping:


- name: Second Play
  gather_facts: no
  tasks:
  hosts: centos
  tasks:
   - name: Ping centos ansible nodes
     ping:
